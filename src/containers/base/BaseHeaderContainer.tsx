import React, { Component } from 'react';
import Header from 'components/common/Header';

export class BaseHeaderContainer extends Component {
  render() {
    return <Header />;
  }
}

export default BaseHeaderContainer;
