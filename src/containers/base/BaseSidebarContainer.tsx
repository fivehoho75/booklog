import React, { Component } from 'react';
import SideBar from 'components/common/SideBar';

export class BaseSidebarContainer extends Component {
  render() {
    return <SideBar />;
  }
}

export default BaseSidebarContainer;
